<Window x:Class="PowerLoop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PowerLoop"
        mc:Ignorable="d"
        Title="PowerLoop" Height="450" Width="800"
        d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" LastChildFill="False">
            <Button Content="Play" Click="Play_Click" MinWidth="150"/>
        </DockPanel>
        <DockPanel Grid.Row="1" Margin="0,5,0,0">
            <Label DockPanel.Dock="Top" Content="Settings"/>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <Button Content="Add Item" MinWidth="100"/>
            </StackPanel>
            <ItemsControl>
                
            </ItemsControl>
        </DockPanel>
    </Grid>
</Window>
